/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://www.cbz-to-pdf.com.br/', // Your production URL
  changefreq: 'daily',               // default change frequency
  priority: 0.7,                     // default priority
  // Other options
};